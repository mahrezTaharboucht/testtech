Restaurer les packets Nugets :
- nlog
- mettre à jour le chemin des logs au niveau de Nlog.config (par défaut : d:/logs)
Référence du service :
 - par défaut :  http://localhost/Services/LemonWayService.asmx 
 - A  adapter/mettre à jour selon le déploiement local : au niveau de Services references => click  droit sur LemonWayService => Configurer la référence du service avec la nouvelle url.